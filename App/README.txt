Pyinstaller is used to package the application into an executable, which is placed in dist\main. 
The build folder is used for debugging.

To package the application, run:
	-pyinstaller main.py

To recompile the application, run:
	-pyinstaller main.spec